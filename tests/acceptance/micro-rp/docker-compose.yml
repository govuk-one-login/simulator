services:
  micro-rp:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - RP_CLIENT_ID=${RP_CLIENT_ID}
    restart: on-failure
    network_mode: host